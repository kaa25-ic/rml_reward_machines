# Randomized LetterEnv

This package will contain the randomized LetterEnv variant. It shares grid
mechanics with `envs.letter_env_core` and keeps randomized-environment
definitions, encodings, monitor specifications, experiments, results, and
analysis local to this package.
