# Multitask LetterEnv

This package will contain the multitask LetterEnv variant. It shares grid
mechanics with `envs.letter_env_core` and keeps multitask-specific task
definitions, encodings, monitor specifications, experiments, results, and
analysis local to this package.
